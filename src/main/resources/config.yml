language: zh-TW

home:
  max-homes: 3
  cooldown-seconds: 30
  teleport-delay-seconds: 3

tpa:
  request-expiry-seconds: 60
  send-cooldown-seconds: 30
  teleport-delay-seconds: 3

warp:
  cooldown-seconds: 30
  teleport-delay-seconds: 3

pet:
  protection-enabled: true

experimental:
  universal-lead-enabled: false
  pet-affection-enabled: false

input:
  timeout-seconds: 60
